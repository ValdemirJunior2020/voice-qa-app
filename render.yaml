services:
  - type: web
    name: voice-qa-server
    env: docker
    rootDir: server
    plan: free
    autoDeploy: true
    healthCheckPath: /
    envVars:
      - key: PYTHON_EXEC
        value: python3
      - key: GEMINI_API_KEY
        sync: false

  - type: web
    name: voice-qa-client
    env: static
    rootDir: client
    buildCommand: npm ci && npm run build
    staticPublishPath: build
    envVars:
      - key: REACT_APP_API
        value: ""  # set this to your server URL in the Render dashboard after server is live
